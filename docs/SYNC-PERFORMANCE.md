# 本轮 GitHub 同步范围与升级

目标仓库：`augety121/jianlitianxie`；分支：`codex/local-vault-quality-20260917`；审查入口：[PR #1](https://github.com/augety121/jianlitianxie/pull/1)。提交之前的基线为 `b66597496e642450c104feb97744c35dc5e725a3`。本文件随实际同步代码提交；精确新提交及 CI 状态以该 PR 为准，不预写未发生的结果。

## 已包含的改动

扫描过程缓存标签、分区和经历锚点；每计划建立匹配索引；菜单／日历事件等待；写前校验目标上下文、选项和遮挡；保留延迟回读；不确定写入停止后续字段；数值指标；完整可复现的性能和安全控件回归，以及 CI 测试／打包。

共享算法位于 `extension/core/semantics.mjs` 和 `planner.mjs`，旧 `bridge` 文件仅兼容导出。扩展仍采用原背景脚本、原页面抽屉和原 MCP 面板。原配对协议／扩展版本保持 0.3.1；内嵌页面引擎 build 独立标为 0.4.1，避免新桥接实例与旧协议握手产生不必要断裂。

## 不在此次同步中

加密资料库更新被工具安全检查拦截后，没有重试绕过，也没有转用编码、工作流或其他通道上传同一受阻内容。原已存在的加密基础保留不动；完整本地工作台、版本管理、脱离 MCP 填写和相应消息改造均不在此分支。不能把上轮本地 0.4.0 包、当前本地开发候选和本分支混作同一产品。

## 升级当前 MCP 安装

1. 先备份私有资料和原代码目录；不要把真实简历、配对码或主档复制到公开仓库。
2. 下载此分支源码／Actions 的 `active-source-review`。必须一起更新 `bridge` 和 `extension`，因桥接现在引用共享 `extension/core`。
3. 在原扩展安装目录替换公开代码，再在 Chrome／Edge 扩展页点重新加载；不要先卸载或清理存储。
4. 在 Codex 重连 `resume_fill`。回到申请网页，重新打开工具栏助手或刷新页面，然后按原“扫描给 Codex → 准备 → 授权填写 → 回读”流程操作。
5. 页面变化、选项改名、遮挡或不确定写入会停止／跳过。检查已填网页再重新扫描，不盲目重试。最后保存、声明与提交仍由本人完成。

如果需要体验旧完整本地工作台，仍需使用上轮独立交付包，不能从此 PR 假定它已上线。旧主档仍为明文；静态基础加密代码的存在不等于实际数据路径已经加密。

## 验证边界

本轮同步分支实际执行 Node 34/34、隔离 Chromium DOM 25/25、新守卫 DOM 16/16；匹配索引与旧候选逻辑做 120×30 字段等价检查。被冻结的 0.4.0 引擎与候选进行三种虚构表单、每种 6 对配对比较，36 次全部独立核对通过。数字不能加总成网站覆盖率。

未登录真实招聘网站、未提交申请、未把真实个人信息加入示例。安装后的 MV3 权限与生命周期、复杂跨域 iframe、全部厂商控件和服务器回执仍需真实验收。CI 的绿色状态只代表所运行的自动化用例。

# 验证记录（2026-09-17）

## 0.3.1 最新进度

- Node 19项通过，包含多选完整集合、明确新增行总数、旧值保护，以及同端口认证桥接复用；HTTP MCP拒绝无效配对码和未知工具。
- 简道云浏览器夹具21项通过：新增行、分行填写、原生多选、菜单重绘后的自定义多选等；均为虚构资料。
- 实际帆软页面此前基础信息、学历和部分经历已回读；本次新增7条奖项及后续批量计划尚未验收成功。更新后扫描未响应，桥接已重启到0.3.1，等待页面重新连接。不得把模拟测试或命令排队当成真实填写完成。

## 已执行
- Node 内置测试：8项通过。覆盖精确匹配、旧值保护、日期精度、冲突和多段经历、候选项、敏感控件排除、MCP默认去值、stdio握手、本机认证与网页Origin拒绝、单次资料分享、授权计划锁定及成功映射记忆。
- Codex内置浏览器隔离夹具：11项通过。覆盖文本、下拉框、多行文本、日期留空、验证码/同意声明跳过、输入事件、回读、旧值变化拒绝、提交次数为0。
- 发布源码隐私检查通过；真实个人档案位于仓库外，不进入源码和发布包。

## 尚未完成的真实环境验收
- 需要本人在Edge加载extension目录，重连Codex MCP后做首次端到端联调。
- 尚未逐一验证北森、智联、Moka、各央企自建系统。
- React受控组件、Ant/Element、自定义单选、级联地区不能仅凭原生夹具宣称兼容。
- 不支持跨域iframe直接填写、自动新增经历、自动上传证件和自动提交。

## 复现
运行 npm test 和 npm run check。
使用本地HTTP服务器在仓库根目录提供 tests/fixture.html，打开页面并点击测试按钮。仅使用虚构测试资料。

不要在真实申请页反复做自动化回归；先用隔离夹具验证，再在本人确认的草稿中检查。

## 0.1.1 连接回归
- 对旧运行服务复现：正确配对码，无Origin返回403；有扩展Origin返回200。
- 修复后隔离桥接回归通过：无Origin+正确配对码200，错误码401；网页Origin及null Origin403。
- 已检查新版桌面布局；扩展实际重载与用户当前网页的端到端连接仍需用户重连后确认。

## 0.1.2 验证
- 9项Node测试通过，新增当前标签绑定、异页计划拒绝、未勾选项不写入的消息路由测试。
- 浏览器隔离夹具验证浮动按钮、抽屉打开、扫描计划（可填/保留/未知三类）、授权后的回读提示。夹具使用虚构资料和模拟消息；为辅助测试，夹具开放shadow root，生产版仍为closed。
- 未把模拟回读当成真实招聘网站的保存成功，实际Edge网申仍需首次现场验证。

## 0.1.3
9项Node测试通过，补充form_context授权边界。浏览器16项隔离检查全部通过：包含单选、年月、隐藏附件检测、文件类型拒绝、原生文件交付、原有输入/旧值/日期精度/不提交保护。文件交付使用虚构内容，未验证真实服务器上传；自定义日期及各厂商控件不宣称全面通过。

信捷北森现场核对：当前站点使用 .form-item > .form-item__control 与 label，已纳入字段标签查找。此前扫描占位符并不代表字段名。现场由Edge连接补全学习形式并回读，点击暂存但无明确回执；没有点击预览并提交。公开记录不包含个人字段值和站点账户参数。

## 0.2.0（2026-09-17）

- Node自动化17项通过，其中集成测试覆盖：本机认证、扫描共享边界、资料维护/备份、历史材料检索、整表草稿、Codex填写排队、标签页归属、过期计划拒绝、写入锁、草稿不学习为固定事实。
- `fixture.html`：浏览器16项检查通过。
- `platform-fixture.html`：浏览器14项检查通过，包含Phoenix标签、Element必填/数字、关联自定义下拉、错序经历、年月、横向表格、多行表头与学校锚点、紧急联系分区、多选转人工、隐藏附件、折叠检测、标签变化拒绝和回读。
- 7份真实材料只在仓库外整理；公开夹具全为虚构值。Node的七平台字段用例验证同义词匹配，不代表七家网站端到端成功。
- 仍需Edge重新加载扩展、MCP重连及具体网站现场验收。此前版本记录保留；当前能力边界以README和PLATFORMS为准。
- 页面抽屉的模拟Codex命令链已在浏览器验证：扫描显示计划，接收批量命令后显示回读确认且禁用重复填写；这是模拟消息验收，不替代真实网站验收。

## 0.2.1 简道云识别修复
- Node 17项通过。新增 jiandaoyun-fixture.html 浏览器16项检查通过：字段标签、表头/行锚点、必填、下拉选项、单选、年月、附件、写入回读与旧值保护，不提交。夹具均为虚构数据。
- 真实帆软页面只读结构检查曾识别38项、0未标注；不等于38项都能自动填写。真实用户扩展仍需重新加载后验收完整填写链。
- 下拉扫描只打开菜单读取实际候选，不选择；依赖父级的字段需要父级填写后再扫描。隐藏分区、未新增经历、跨域iframe不宣称全部覆盖。
- 更新后点工具栏图标可替换旧浮窗，保留网页输入；配对码仍沿用storage.local。

## 0.2.2 现场后续
- 自有MCP真实填写确认出生年月、实习三项、奖项名称；已有五项基础信息保留。下拉及单选没有成功回读，不能记为成功。
- 补充mousedown/mouseup与内部标签触发，回传失败原因及engineVersion。Node17项、简道云夹具16项通过；新版实际选择动作仍待扩展重新加载后验收。
